import type { Config } from 'tailwindcss'

export default <Partial<Config>>{
    theme: {
        colors: {
            'midnight': '#03071E',
            'offwhite': '#E9ECEF',
            'lime': '#70F996'
        },
        fontFamily: {
            'poppins': ['Poppins', 'sans-serif'],
        }
    }
}